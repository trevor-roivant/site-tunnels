from setuptools import find_packages, setup

setup(
    name='netbox-site-tunnels',
    version='0.1',
    description='Keep inventory of tunnels',
    install_requires=[],
    packages=find_packages(),
    
    include_package_data=True,
    zip_safe=False,
)